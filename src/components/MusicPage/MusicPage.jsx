import React from 'react';
import classes from './MusicPage.module.css'


const MusicPage = (props) => {
    return (
        <div className={classes.MusicPageWrapper}>
            <h1>Music Page (=</h1>
        </div>
    );
};

export default MusicPage;
